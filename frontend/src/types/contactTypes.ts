export interface ContactFormInputTypes {
  name: string;
  email: string;
  message: string;
}

export interface ContactInfo {
  address: string;
  phone: string;
  email: string;
  hours: string;
  coordinates: [number, number];
}
